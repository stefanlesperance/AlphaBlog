<!-- Something unique about this, because its under the categories controller, I can call it
 anywhere else, and in the same process, have access to the variable ITSELF. -->
<%= link_to category.name, category_path(category), class: "badge badge-info mx-1" %>