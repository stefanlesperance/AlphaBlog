<div id = "page-content">
<h1 class = "text-center mt-4">Showing All Articles</h1>


<% @articles.each do |article|  %>
<div class = "container ">
	<div class="row justify-content-center">
		<div class="col-8 mt-4">
				<div class="card text-center shadow p-3 mb-5 bg-white rounded">
					<div class="card-header">
						Featured
					</div>
					<div class="card-body">
						<h5 class="card-title"><%= article.title%></h5>
						<p class="card-text"><%= article.description%></p>
						<div class="btn-group" role="group" aria-label="Basic example">
							<%= button_to 'Show', article_path(article) %>
							<%= button_to 'Edit', article_path(article) %>
							<%= button_to 'Delete', article_path(article), method: :delete %>
						</div>
					</div>
					<div class="card-footer text-muted">
						Edited About
					</div>
			</div>
		</div>
	</div>
</div>

<% end %>

</div>
<!-- <table>
	<thead>
		<tr>
			<th>Title</th>
			<th>Description</th>
			<th colspan = "3">Actions</th>
		</tr>
	</thead>

	<tbody>
		<% @articles.each do |article|  %>
		<tr>
			<td><%= article.title%></td>
			<td>c</td>
			<td><%= link_to 'Show', article_path(article) %></td>
			<td><%= link_to 'Show', article_path(article) %></td>
			<td><%= link_to 'Delete', article_path(article), method: :delete %></td>
		</tr>
		<% end %>	
	</tbody>
</table> -->
<br>
	<p>
		<%= link_to 'Create Article', new_article_path %>
	</p>